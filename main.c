#include <fcntl.h>
#include "pipex.h"

int main(int argc, char *argv[], char *envp[])
{
	int *a = malloc(1000000 *4);

	scanf("%d", a);
	exit(1);
	/*char	**paths;
	int	i;

	i = 0;	
	paths = get_paths(envp, argv[1]);*/

}	

